# Core app + test dependencies. Resolved on 2026-07-12 (Python 3.12, CPU-only).
# Eval-only dependencies (Ragas + LangChain judge stack) live in
# requirements-eval.txt so the app/CI install stays lean and robust.
sentence-transformers==5.6.0
chromadb==1.5.9
pymupdf==1.28.0
python-docx==1.2.0
rank-bm25==0.2.2
python-dotenv==1.2.2
httpx==0.28.1

# API (M5)
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# Dev / test
pytest==9.1.1
ruff==0.15.21
