# Copy to .env and fill in. Never commit .env.

# --- Generation (needed from Milestone 2 on) ---
# Which ChatProvider to use: gemini | groq | openai
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GROQ_API_KEY=
OPENAI_API_KEY=

# --- Embeddings / retrieval (defaults are fine) ---
# EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# RAG_DATA_DIR=./data
# CHUNK_SIZE_TOKENS=500
# CHUNK_OVERLAP_TOKENS=50
# TOP_K=5
