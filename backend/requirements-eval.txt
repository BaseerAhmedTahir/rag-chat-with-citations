# Eval-only dependencies (Milestones 3–4): Ragas + the LangChain judge stack.
# Install on top of the core app deps:  pip install -r requirements-eval.txt
-r requirements.txt

# ragas 0.4.x is incompatible with langchain-community 0.4.x (it imports the
# removed vertexai chat model), so the whole langchain stack is pinned to 0.3.x.
ragas==0.3.9
langchain-google-genai==2.1.12
langchain-huggingface==0.3.1
langchain-community==0.3.31
langchain-groq==0.3.8
pandas==3.0.3
tabulate==0.10.0
