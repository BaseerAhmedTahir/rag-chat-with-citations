# Slim deploy stack for memory-constrained free hosting (Render free, 512 MB).
# Same bge-small-en-v1.5 model as the full stack, but int8 ONNX via fastembed —
# no torch/transformers. Run with EMBEDDER_KIND=fastembed RETRIEVER_KIND=hybrid.
# Measured footprint: ~300 MB working set serving queries.
chromadb==1.5.9
pymupdf==1.28.0
python-docx==1.2.0
rank-bm25==0.2.2
python-dotenv==1.2.2
httpx==0.28.1
fastembed==0.8.0

# API
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
